{
  "name": "condor-motors",
  "private": true,
  "version": "1.0.0",
  "description": "Sistema de gestión de empleados, inventario y facturación.",
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "build:server": "npm run build --workspace server",
    "start:server": "npm run start --workspace server",
    "type-check:server": "npm run type-check --workspace server",
    "init:server": "npm run init --workspace server",
    "lint:server": "npm run lint --workspace server",
    "db:generate": "npm run db:generate --workspace server",
    "db:migrate": "npm run db:migrate --workspace server",
    "db:push": "npm run db:push --workspace server",
    "db:seed": "npm run db:seed --workspace server",
    "db:reset": "npm run db:reset --workspace server",
    "db:populate": "npm run db:populate --workspace server",
    "db:studio": "npm run db:studio --workspace server",
    "build": "npm run build --workspace server",
    "start": "npm run start --workspace server"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "server"
  ],
  "dependencies": {
    "drizzle-orm": "^0.39.3"
  }
}
